import mongoose from 'mongoose';
import { stringify } from 'querystring';

export default new mongoose.Schema({
    _id: {
        type: String,
    },
    username: {
        type: String,
        required: true,
        unique: true
    },
    firstname: {
        type: String,
        required: true
    },
    lastname: {
        type: String,
        required: true
    },
    email: {
        type: String,
        required: true,
        unique: true
    },
    password: {
        type: String,
        required: true
    }
});