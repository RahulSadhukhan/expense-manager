{
  "name": "expense-manager",
  "version": "1.0.0",
  "description": "Application for managing your expenses",
  "main": "server/src/index.js",
  "scripts": {
    "start": "node ./server/build/index.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client && NPM_CONFIG_PRODUCTION=false npm install --prefix server && npm run build --prefix server"
  },
  "author": "Rahul",
  "license": "ISC"
}
